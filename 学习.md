# CoolCap UniApp 项目学习文档（YL 分支）

## 一、项目概述

本仓库在 **YL 分支** 上重塑为 **CoolCap 化疗头皮冷却冰帽移动端**：  
患者/护理人员通过手机查看头皮分区温度、控制治疗方案、管理档案与报警。  

`master` 分支仍保留原企业后台管理系统前端；后端 `uniapp-server` **同时挂载**旧接口与 CoolCap 新接口，两套前端可共用同一后端与同一 MySQL 库。

| 维度 | 技术选型 |
|------|---------|
| 前端框架 | uni-app + Vue 3 (Composition API) + TypeScript |
| UI 库 | uview-plus |
| 构建工具 | Vite 5（H5 base：`/coolcap/`） |
| 后端框架 | FastAPI |
| ORM / 库 | SQLAlchemy 2.x + MySQL（`study_uniapp`） |
| CoolCap 表前缀 | `YL_` |
| 认证 | JWT（`/api/auth`）；YL 演示可回退 mock 会话 |
| 设备层 | Mock BLE + 本地 PID/露点/安全模拟（预留真机 BLE） |

---

## 二、目录结构（YL）

```
xiaomi-uniapp/
├── uniapp-web/                     # CoolCap 前端
│   ├── src/
│   │   ├── api/
│   │   │   ├── request.ts          # 统一请求；mock 不因 401 踢登录
│   │   │   ├── auth.ts             # 登录/当前用户
│   │   │   └── coolcap.ts          # 方案/会话/报警/安全...
│   │   ├── pages/
│   │   │   ├── login/login.vue
│   │   │   ├── home/home.vue       # 治疗 Tab
│   │   │   ├── monitor/monitor.vue # 监控 Tab
│   │   │   ├── protocol/           # 方案 Tab + 编辑
│   │   │   ├── archive/            # 档案 Tab + 详情
│   │   │   ├── alarm/alarm.vue
│   │   │   └── profile/profile.vue
│   │   ├── components/             # ZoneHeatmap / ZoneCard / PageBack...
│   │   ├── services/
│   │   │   ├── types.ts            # ZoneId、会话、方案类型
│   │   │   ├── ble/device.ts       # MockBleDevice
│   │   │   ├── control/            # dewpoint / pidSim / safety
│   │   │   └── store/              # therapyStore / useTherapy
│   │   ├── pages.json              # 路由与 tabBar
│   │   └── uni.scss                # uview-plus 主题变量
│   └── vite.config.ts
│
├── uniapp-server/                  # 后端（旧 + 新）
│   ├── models/coolcap.py           # YL_* 模型
│   ├── routers/coolcap.py          # /api/coolcap/*
│   ├── routers/auth.py 等          # master 旧路由仍保留
│   ├── sql/YL_coolcap_tables.sql
│   ├── scripts/init_yl_tables.py
│   └── main.py                     # 同时 include 两套路由
│
├── deploy/                         # Nginx 与上线说明
└── docs/compose/spec/coolcap-mobile.md
```

---

## 三、前端要点

### 3.1 路由与 TabBar

`pages.json` 中：

- **Tab**：治疗 / 监控 / 方案 / 档案 / 我的  
- **子页**：报警中心、方案编辑、档案详情（带左上角 `PageBack`）

登录页为首屏；有 token 才进入主 Tab。

### 3.2 请求层 `request.ts`

- `BASE_URL = '/uniapp-api'`，部署时由 Nginx 反代到后端  
- **mock 会话**（`cc_mock_auth`）：后端 401 / 网络失败 **不跳登录**，保证演示可用  
- 真实登录成功后清除 mock 标记  

### 3.3 Mock BLE 与控制模拟

`services/ble/device.ts` 中 `MockBleDevice`：

- 6 区：`F / TL / TR / P / OL / OR`  
- 每秒步进：PID → 功率 → 头皮温度近似  
- 露点钳制、热端/冻伤软保护、接触与传感器故障开关  

`useTherapy.ts` 订阅设备快照，结束治疗时：

1. 写入本地 Storage 档案  
2. 若已 `startSession` 拿到远端 id，则 `finishSession` 批量写入 `YL_SESSION*`  

### 3.4 签名 UI

- 头皮俯视 **热力图**（颜色映射温度）  
- 分区卡片（目标/实测/功率/协调提示）  
- 露点安全余量条  
- 历史列表「已入库 / 本地」标签  

### 3.5 常用命令

```bash
cd uniapp-web
npm install
npm run dev:h5      # http://localhost:5173/coolcap/
npm run build:h5    # dist/build/h5
```

uview-plus 需要 `src/uni.scss` 引入 `theme.scss`，否则组件样式报 `$u-primary` 未定义。

---

## 四、后端要点

### 4.1 双栈路由

`main.py` 同时注册：

| 来源 | 前缀示例 | 使用方 |
|------|----------|--------|
| master | `/api/auth` `/api/ent/*` `/api/gold/*` `/api/laptops/*` `/api/weather/*` ... | master 前端 |
| YL | `/api/auth` `/api/coolcap/*` | CoolCap 前端 |

### 4.2 CoolCap 主要接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/PUT/DELETE | `/api/coolcap/protocols` | 方案（内置只读） |
| GET | `/api/coolcap/sessions` | 档案分页 |
| POST | `/api/coolcap/sessions/start` | 开始（算露点） |
| POST | `/api/coolcap/sessions/{id}/finish` | 归档（写采样点） |
| GET | `/api/coolcap/sessions/{id}/export` | 导出结构 |
| POST | `/api/coolcap/sessions/{id}/reproduce` | 一键复现参数 |
| GET/POST | `/api/coolcap/alarms` | 报警 |
| GET | `/api/coolcap/safety` | 安全包络 |

### 4.3 模型注意

- 表名 `YL_*`；类名如 `YLSession`、`YLDevice`  
- **外键与 relationship 必须配对**，例如 `device_id` 要有  
  `ForeignKey("YL_DEVICE.id")`，否则启动 mapper 报：  
  `Could not determine join condition between parent/child tables`  
- `create_all` 启动自动建表；也可执行  
  `python scripts/init_yl_tables.py`  

### 4.4 本地启动

```bash
cd uniapp-server
# 建议使用项目约定 Python，例如：
# D:\python\Python\Python310\python.exe
python -m uvicorn main:app --host 0.0.0.0 --port 8022
```

健康检查：`GET /api/health`  
接口文档：`/docs`

---

## 五、数据库学习（YL_）

| 表 | 学习点 |
|----|--------|
| `YL_PROTOCOL` + `YL_PROTOCOL_STAGE` | 一对多；阶段排序 `sort_order` |
| `YL_SESSION` | 会话主档；`protocol_snapshot` JSON 快照 |
| `YL_SESSION_POINT` | 高频采样；按 `session_id+ts` 索引 |
| `YL_ALARM` | 分级 `info/warning/critical` |
| `YL_CONTROL_COMMAND` | 下行指令审计，便于追溯 |

业务路径建议先看：

1. 登录拿 JWT  
2. `POST /sessions/start`  
3. 前端模拟采集  
4. `POST /sessions/{id}/finish`  
5. `GET /sessions` + 档案详情  

---

## 六、部署学习

1. **前端**：上传 `dist/build/h5`，站点路径 `/coolcap/`  
2. **反代**：`/uniapp-api/` → 后端 `http://127.0.0.1:8022/`（去掉前缀）  
3. **后端**：Docker 或 uvicorn 常驻；连接现有库 `study_uniapp`  
4. **双前端**：master 与 YL 静态资源可分目录部署，共用同一 API 域名  

参考：`deploy/README.md`、`deploy/nginx-coolcap.conf`

---

## 七、常见问题

| 现象 | 原因与处理 |
|------|------------|
| 登录后马上跳回登录 | 旧逻辑 401 清 token；已改为 mock 会话不踢人，刷新后验证 |
| 后端启动 mapper 错误 | `relationship` 无外键；补 `ForeignKey` 后重启 |
| uview 组件样式 `$u-primary` | 缺少 `uni.scss` 主题导入 |
| 打包路径 404 | `vite` base 为 `/coolcap/`，部署目录需匹配 |
| 娱乐/黄金接口 404 | 后端未从含旧路由的版本部署；YL 的 `main.py` 已双挂载，确认容器镜像已更新 |

---

## 八、建议学习顺序

1. 跑通 H5 登录 → 连接模拟设备 → 开始/结束治疗  
2. 读 `pages.json` 与 `home.vue`  
3. 读 `services/ble/device.ts` 与 `control/*`  
4. 对照 `routers/coolcap.py` 与 `YL_*` 模型  
5. 结束治疗后查数据库 `YL_SESSION` / `YL_SESSION_POINT`  
6. 再对照 master 分支理解旧后台差异  

---

*本文件描述 **YL 分支**。master 分支请使用 master 上的 README/学习文档。*
